# TODO

## Fix mobile right scrolling + mobile navbar

- [ ] Add global CSS to prevent horizontal overflow and improve touch scrolling.
- [ ] Fix Header mobile menu to lock body scroll while open (avoid right scrolling caused by background page).
- [ ] Adjust mobile menu width/position to avoid any off-canvas overflow (85% can exceed viewport with padding).
- [ ] Ensure menu panel uses `right: 0` (or translate) and `w-screen max-w` patterns so it never exceeds viewport.
- [ ] Run typecheck/lint/build to confirm no regressions.

